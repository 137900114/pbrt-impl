add_subdirectory(spdlog)
add_subdirectory(stb)
add_subdirectory(assimp)
add_subdirectory(optick)
add_subdirectory(mmd)
add_subdirectory(tinyxml2)


message(STATUS "add ${CMAKE_CURRENT_SOURCE_DIR}/assimp/include to include directory")
target_include_directories(Alex_lib PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/assimp/include)
message(STATUS "add ${CMAKE_CURRENT_SOURCE_DIR}/spdlog/include to include directory")
target_include_directories(Alex_lib PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/spdlog/include)
message(STATUS "add ${CMAKE_CURRENT_SOURCE_DIR}/stb/include to include directory")
target_include_directories(Alex_lib PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/stb/include)
message(STATUS "add ${CMAKE_CURRENT_SOURCE_DIR}/optick/src to include directory")
target_include_directories(Alex_lib PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/optick/include)
message(STATUS "add ${CMAKE_CURRENT_SOURCE_DIR}/mmd/include to include directory")
target_include_directories(Alex_lib PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/mmd/include)
message(STATUS "add ${CMAKE_CURRENT_SOURCE_DIR}/tinyxml2 to include directory" )
target_include_directories(Alex_lib PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/tinyxml2)

if(ENABLE_TEST)
    add_subdirectory(googletest)
    set(GTEST_INCLUDE ${CMAKE_CURRENT_SOURCE_DIR}/googletest/googletest/include CACHE INTERNAL "GTEST_INCLUDE") 
    message(STATUS "add ${GTEST_INCLUDE} to include directory")
endif()